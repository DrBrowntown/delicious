mixin forgotForm()
    form(action="/account/forgot" method ="POST")
        h2 I forgot my pasword
        label(for="email") Email
        input(type="email" name="email")
        input.button(type="submit" value="Send a reset request")